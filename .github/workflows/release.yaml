name: release
on:
  pull_request:
    branches:
      - master
    types: [closed]
jobs:
  release:
    name: add tag
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: checkout
        uses: actions/checkout@v2
        